---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hermesagents.hermes.nousresearch.com
spec:
  group: hermes.nousresearch.com
  names:
    kind: HermesAgent
    listKind: HermesAgentList
    plural: hermesagents
    shortNames:
    - ha
    singular: hermesagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HermesAgentSpec defines the desired state of HermesAgent.
            properties:
              apiServer:
                description: |-
                  APIServer controls the optional OpenAI-compatible API server exposed by the
                  Hermes gateway.
                properties:
                  enabled:
                    default: false
                    description: Enabled starts the API server inside `hermes gateway
                      run`.
                    type: boolean
                  keySecretRef:
                    description: KeySecretRef points to the API_SERVER_KEY value.
                      Required when enabled.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        description: |-
                          Name of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  port:
                    default: 8642
                    description: Port is the API server listen port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: apiServer.keySecretRef.name and apiServer.keySecretRef.key
                    are required when apiServer.enabled is true
                  rule: '!has(self.enabled) || self.enabled == false || (has(self.keySecretRef)
                    && has(self.keySecretRef.name) && size(self.keySecretRef.name)
                    > 0 && has(self.keySecretRef.key) && size(self.keySecretRef.key)
                    > 0)'
              config:
                description: |-
                  Config is written as the initial config.yaml when the data PVC does not
                  already contain one. It intentionally preserves Hermes config keys without
                  the operator trying to mirror the Python config schema.
                x-kubernetes-preserve-unknown-fields: true
              dashboard:
                description: Dashboard controls the optional in-container dashboard
                  side process.
                properties:
                  enabled:
                    default: false
                    description: |-
                      Enabled starts `hermes dashboard` as a side process in the Hermes
                      container entrypoint.
                    type: boolean
                  port:
                    default: 9119
                    description: Port is the dashboard listen port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  tui:
                    default: false
                    description: TUI enables the embedded browser chat tab.
                    type: boolean
                type: object
              envFromSecrets:
                description: |-
                  EnvFromSecrets references Kubernetes Secrets to expose as environment
                  variables. The CRD has no plaintext secret fields.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              image:
                description: Image is the Hermes container image to run.
                minLength: 1
                type: string
              imagePullPolicy:
                description: ImagePullPolicy controls when Kubernetes pulls the Hermes
                  image.
                enum:
                - Always
                - Never
                - IfNotPresent
                type: string
              persistence:
                description: Persistence configures the PVC mounted as HERMES_HOME
                  at /opt/data.
                properties:
                  retainOnDelete:
                    default: true
                    description: RetainOnDelete keeps the PVC after the HermesAgent
                      CR is deleted.
                    type: boolean
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    description: Size is the requested PVC size.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              resources:
                description: Resources configures CPU and memory requests/limits for
                  the Hermes pod.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              soul:
                description: |-
                  Soul is written as the initial SOUL.md when the data PVC does not already
                  contain one.
                type: string
            required:
            - image
            type: object
          status:
            description: HermesAgentStatus defines the observed state of HermesAgent.
            properties:
              conditions:
                description: Conditions reports readiness and validation details.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              internalURLs:
                description: InternalURLs lists in-cluster URLs for enabled HTTP surfaces.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is a short machine-readable summary of the current
                  lifecycle state.
                type: string
              pvcName:
                description: PVCName is the managed data PVC name.
                type: string
              readyReplicas:
                description: ReadyReplicas is copied from the managed StatefulSet
                  status.
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the managed ClusterIP service name, when
                  any port is enabled.
                type: string
              statefulSetName:
                description: StatefulSetName is the managed workload name.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
